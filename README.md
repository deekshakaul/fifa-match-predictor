# fifa-match-predictor
FIFA World Cup 2026 match predictor using OpenAI Responses API with web search and persistent context management.

## Setup
- Copy `.env.example` to `.env` and add your OpenAI API key
- `pip install openai pyyaml python-dotenv`
